def patch(decompiled_dir: str) -> bool:
    """
    WhatsApp patching is handled at APK level in pre_patch.py.
    Keep this as a no-op decompiled stage to satisfy the shared pipeline.
    """
    return True
